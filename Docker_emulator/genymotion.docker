FROM alvrme/alpine-android-base

ENV BUILD_TOOLS "26.0.3"
ENV TARGET_SDK "26"

# Install SDK Packages
RUN ${ANDROID_HOME}/tools/bin/sdkmanager "build-tools;${BUILD_TOOLS}" "platforms;android-${TARGET_SDK}"

RUN cd /opt/sdk/tools/bin/

CMD ["avdmanager list"]